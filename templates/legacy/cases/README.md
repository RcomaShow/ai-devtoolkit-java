# Legacy Cases

Create one folder per stable legacy slice, for example:

- `verifica-capacita`
- `caricamento-nomina`
- `matching-annullamento`

Inside each case folder, keep the long-lived markdown reports at the top level and write regenerated raw outputs under `generated/<run-id>/`.