# Cursor Adapter

**Status:** Placeholder — not implemented.

This adapter exists as an extensibility point for Cursor IDE integration. The current runtime is Copilot-first.

## When to populate
- When Cursor is actively used alongside VS Code + Copilot.
- Required assets: `.cursor/rules/` with workspace conventions, shared skill references.
