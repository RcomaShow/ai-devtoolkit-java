# Claude Code Adapter

**Status:** Placeholder — not implemented.

This adapter exists as an extensibility point for when Claude Code becomes an active secondary tool in the workspace. The current runtime is Copilot-first; Claude uses `CLAUDE.md` at the workspace root as an alias to `AGENTS.md`.

## When to populate
- When Claude Code is actually used as a development tool alongside Copilot.
- Required assets: `CLAUDE.md` workspace bridge, `.claude/settings.json`, and mirrored skill references.
